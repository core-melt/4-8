<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>投稿詳細</title>
</head>
<body>
	<%= flash[:success] %>
	<dir>
		<p>Title:<%= @book.title %></p>
		<p>body:<%= @book.body %></p>
		<%= link_to "Edit", edit_book_path(@book.id) %>
		<%= link_to "Back", books_path %>
	</dir>
</body>
</html>